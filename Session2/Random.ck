//Random numbers
Math.srandom(123); //add fixed seed
/* ============ */

<<<Math.random()>>>; //generates an int between Math.randommax
<<<Math.random2(-15,15)>>>; // takes a range as an input and generates between the two
<<<Math.randomf()>>>; // random floating point between [0,1]
<<<Math.random2f(-1.0,1.0)>>>; // random floating point between [min, max]